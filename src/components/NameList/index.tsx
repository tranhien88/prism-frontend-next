import { HorseProfileProps } from '@interfaces'
interface NameListProps {
  lists: HorseProfileProps[]
  enableInfiniteScroll?: boolean
  showLoading?: boolean
  selectedProfile?: HorseProfileProps
}
export default function NameList({ lists, } : NameListProps) {
  return (
    <>
      
    </>
  );
}
